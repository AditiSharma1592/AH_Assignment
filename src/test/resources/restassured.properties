# RestAssured Configuration Properties
# These properties help handle SSL issues across different environments

# SSL Configuration
restassured.ssl.relaxedHTTPSValidation=true
restassured.ssl.allowAllHostnames=true

# Connection Configuration
restassured.connection.timeout=10000
restassured.read.timeout=10000

# Logging (optional)
restassured.logging.enable=true
